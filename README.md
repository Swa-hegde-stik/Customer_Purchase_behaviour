# Customer Shopping Behavior Analysis

##  Project Overview

This project analyzes customer shopping behavior using transactional data from
3,900 purchases across multiple product categories.

The objective is to identify customer spending patterns, product preferences,
discount behavior, subscription trends, and customer segments, and convert
these findings into actionable business insights.

---

##  Tech Stack

- Python
- Pandas
- NumPy
- PostgreSQL
- SQL
- SQLAlchemy
- Power BI
- Jupyter Notebook

---

##  Project Workflow
```
Raw Dataset
     ↓
Python Data Cleaning
     ↓
Feature Engineering
     ↓
PostgreSQL
     ↓
SQL Business Analysis
     ↓
Power BI Dashboard
     ↓
Business Insights
```
PostgreSQL was used to answer business questions related to revenue,customers, products, discounts, subscriptions, and purchasing behavior.

1.Revenue by Gender
```
SELECT
    gender,
    SUM(purchase_amount) AS revenue
FROM customer
GROUP BY gender
ORDER BY revenue DESC;
```
This analysis compares the total revenue generated by different customer gender groups.

2.High-Spending Discount Users
  Customers who used discounts but spent above the average purchase amount were identified.
```
SELECT
    customer_id,
    purchase_amount
FROM customer
WHERE discount_applied = 'Yes'
  AND purchase_amount > (
      SELECT AVG(purchase_amount)
      FROM customer
  );
```
3.Top 5 Products by Rating
```
SELECT
    item_purchased,
    ROUND(AVG(review_rating), 2) AS avg_rating
FROM customer
GROUP BY item_purchased
ORDER BY avg_rating DESC
LIMIT 5;
```
4.Shipping Type Analysis
  Average purchase amounts were compared across shipping types.
```
SELECT
    shipping_type,
    ROUND(AVG(purchase_amount), 2) AS avg_purchase_amount
FROM customer
GROUP BY shipping_type;
Subscriber vs Non-Subscriber Analysis
SELECT
    subscription_status,
    COUNT(*) AS total_customers,
    ROUND(AVG(purchase_amount), 2) AS avg_spend,
    SUM(purchase_amount) AS total_revenue
FROM customer
GROUP BY subscription_status;
```
5.Customer Segmentation

  Customers were classified into:
  
New

Returning

Loyal

based on their purchase history.
```
SELECT
    CASE
        WHEN previous_purchases = 0 THEN 'New'
        WHEN previous_purchases <= 5 THEN 'Returning'
        ELSE 'Loyal'
    END AS customer_segment,
    COUNT(*) AS number_of_customers
FROM customer
GROUP BY customer_segment;
```

6.Top 3 Products per Category

Window functions were used to identify the most purchased products
within each category.
```
WITH ranked_products AS (
    SELECT
        category,
        item_purchased,
        COUNT(*) AS total_orders,
        RANK() OVER (
            PARTITION BY category
            ORDER BY COUNT(*) DESC
        ) AS item_rank
    FROM customer
    GROUP BY category, item_purchased
)

SELECT
    category,
    item_purchased,
    total_orders,
    item_rank
FROM ranked_products
WHERE item_rank <= 3
ORDER BY category, item_rank;
```
6. Power BI Dashboard

The analyzed data was connected to Power BI to create an interactive
customer behavior dashboard.

Dashboard includes:
Total Customers

Average Purchase Amount

Average Review Rating

Revenue by Category

Sales by Category

Revenue by Age Group

Sales by Age Group

Subscription Status

Shipping Type

Gender

Category filters

7. Business Recommendations

Based on the analysis:

Boost Subscriptions: Promote exclusive benefits for subscribers.

Customer Loyalty: Reward repeat buyers to encourage loyalty.

Review Discount Policy: Balance sales growth with margin considerations.

Product Positioning: Promote highly rated and best-selling products.

Targeted Marketing: Focus campaigns on high-revenue customer segments.

8. Skills Demonstrated
Python

Pandas

NumPy

SQL

PostgreSQL

SQLAlchemy

Data Cleaning

Exploratory Data Analysis

Feature Engineering

Customer Segmentation

Business Analysis

Power BI

Data Visualization

## Author

Swastik Venkatraman Hegde

LinkedIn: linkedin.com/in/swas25

GitHub: github.com/Swa-hegde-stik

Kaggle: kaggle.com/swas25
